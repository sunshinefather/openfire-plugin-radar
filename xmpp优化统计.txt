1.处理会话列表中群聊群名称等信息
  解决方案：会话列表中添加目标类型字段1,单聊；2，群聊。

2.接受通知用message,收到好友请求用message

3.发送通知走http,分定向发送和广播发送,添加是否走离线,提供公共通知拉取策略

4.存储改用nosql存储

5.IQ请求返回原始请求的参数,id,type为必传参数,回应的IQ保留请求的ID属性,error类型的IQ必须包含一个<error/>元素

6.处理发送单聊或群聊的空接受者,空消息 错误

7.添加清空房间，保留管理员

8.用户和组,组成员整合到同一数据库(业务数据库)，方便openfire查看组信息，省略业务系统访问openfire业务,减少服务器之间调用的开销，弱化掉imcrm

9.添加群成员入群时间，方便新加入的成员只能查看加入群之后的群聊信息

10.聊天消息-扩展消息生成全局唯一和当前会话ID

11.剩下的只有聊天记录和会话了，存储在垂直搜索引擎中,方便检阅,提升查询效率，提升并发量，减少关系系数据库的压力

12.mesage 消息撤销掉from属性，防止消息发送者伪造，从登陆连接中获取发送者。

13.提供根据用户登陆名和群id删除群成员

14.提供拉取群组列表时,传入群组类型。根据类型拉取。默认全部

15.ios用户设备号绑定持久存储改为redis或其他三方存储,减少系统内存开销

16.开发websocket访问功能方便web浏览器使用